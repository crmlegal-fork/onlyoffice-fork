# Copiar a .env y rellenar
# JWT_SECRET DEBE ser el mismo que en docker/.env.local
JWT_SECRET=

# Host accesible desde DENTRO del contenedor del DocumentServer
# - En Docker Desktop: host.docker.internal:3000
# - En EC2: el dominio publico de tu CRM
PUBLIC_HOST=host.docker.internal:3000

PORT=3000
